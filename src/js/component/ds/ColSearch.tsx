import React from 'react';
import { Filter, SearchFilterProposal } from '../../model/ds';

interface ColSearchProps {
  filterProposal: SearchFilterProposal;
}

const ColSearch = ({ filterProposal }: ColSearchProps) => {
  return <>TBD</>;
}

export default ColSearch;
