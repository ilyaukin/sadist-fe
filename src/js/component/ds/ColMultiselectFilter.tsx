import React from 'react';
import { MultiselectFilterProposal } from '../../model/ds';

interface ColMultiselectFilterProps {
  filterProposal: MultiselectFilterProposal;
}

const ColMultiselectFilter = ({ filterProposal }: ColMultiselectFilterProps) => {
  return <>TBD</>
}

export default ColMultiselectFilter;
